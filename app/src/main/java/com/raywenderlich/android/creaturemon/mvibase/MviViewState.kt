package com.raywenderlich.android.creaturemon.mvibase

/**
 * This interface creates a type that we'll use to tag view state classes with &
 * doesn't need any functions.
 */
interface MviViewState {
}